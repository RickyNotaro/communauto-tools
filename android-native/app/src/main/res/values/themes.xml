<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Minimal splash theme — Compose handles all Material 3 theming at runtime -->
    <style name="Theme.CommunautoNative" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
