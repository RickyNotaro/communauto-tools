<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme: Material 3 with no action bar (Compose handles chrome) -->
    <style name="Theme.CommunautoNative" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
